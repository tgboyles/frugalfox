# API Base URL
# NOTE: This is used by the browser client, not the container
# The browser makes requests to the backend on the host machine (localhost:8080)
# If deploying behind a reverse proxy, update this to the public backend URL
VITE_API_BASE_URL=http://localhost:8080
