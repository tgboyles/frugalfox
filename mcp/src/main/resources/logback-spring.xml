<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- File appender for all logs -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${user.home}/.frugalfoxmcp/logs/frugalfoxmcp.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${user.home}/.frugalfoxmcp/logs/frugalfoxmcp.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger - only file logging, no console output to keep STDIO clean for MCP communication -->
    <root level="INFO">
        <appender-ref ref="FILE"/>
    </root>

    <!-- Reduce verbosity of Spring Framework logs -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.springframework.boot" level="WARN"/>
    <logger name="org.springframework.ai" level="INFO"/>

    <!-- Application logs -->
    <logger name="com.tgboyles.frugalfoxmcp" level="DEBUG"/>
</configuration>
