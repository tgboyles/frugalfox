services:
  postgres:
    image: postgres:17-alpine
    container_name: frugalfox-postgres
    environment:
      POSTGRES_DB: frugalfox
      POSTGRES_USER: frugalfox
      POSTGRES_PASSWORD: frugalfox
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U frugalfox"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - frugalfox-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: frugalfox-backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/frugalfox
      SPRING_DATASOURCE_USERNAME: frugalfox
      SPRING_DATASOURCE_PASSWORD: frugalfox
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - frugalfox-network

  mcp:
    build:
      context: ./mcp
      dockerfile: Dockerfile
    container_name: frugalfox-mcp
    environment:
      FRUGALFOX_API_BASE_URL: http://backend:8080
      FRUGALFOX_API_TIMEOUT: 30000
    ports:
      - "8081:8081"
    depends_on:
      - backend
    networks:
      - frugalfox-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frugalfox-frontend
    ports:
      - "3000:4173"
    depends_on:
      - backend
    networks:
      - frugalfox-network

  ollama:
    image: ollama/ollama:latest
    container_name: frugalfox-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - frugalfox-network
    healthcheck:
      test: ["CMD-SHELL", "ollama --version > /dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  postgres_data:
  ollama_data:

networks:
  frugalfox-network:
    driver: bridge
